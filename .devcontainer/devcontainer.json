{
    "name": "latex-env",
    "extensions": [
        "James-Yu.latex-workshop"
    ],
    "dockerFile": "Dockerfile",
    // "mounts": [
        // "source=${localEnv:HOME}${localEnv:USERPROFILE}/.gitconfig,target=~/.gitconfig,type=bind,consistency=cached",
        // "source=${localWorkspaceFolder}/tex,target=/tex,type=bind,consistency=cached"
    // ]
    "workspaceMount": "source=${localWorkspaceFolder}/tex,target=/tex,type=bind,consistency=cached",
    "workspaceFolder": "/tex"
} 